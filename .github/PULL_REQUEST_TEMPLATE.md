## Summary

(What this PR changes, and which stage it affects: rtl-from-spec / verify-rtl / synth-pnr / signoff-gds / scripts.)

## Verification

How it was verified — at minimum, one of:

- [ ] `python tests/test_scripts.py` passes locally
- [ ] The affected script run manually against `examples/counter` (state the exit code)
- [ ] No testable behavior changed (docs/templates only)

If a new design or RTL was added: state lint/sim exit codes; synth results if available.
